# 6_exclusions.R
# exclusion numbers during the data collection phases
# June 2025
library(rmarkdown)

render(input = "6_exclusions.Rmd",
       output_format = 'word_document',
       output_dir = 'results', # into separate results directory
       output_file = "6_exclusions.docx")